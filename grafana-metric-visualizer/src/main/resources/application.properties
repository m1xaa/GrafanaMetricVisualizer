
prometheus.uid=${PROMETHEUS_UID}
prometheus.type=prometheus

dashboard.refresh=5s
dashboard.schema.version=41

output.path=grafana-dashboard.json

grafana.api.url=${GRAFANA_API_URL}
grafana.username=${GRAFANA_USERNAME}
grafana.password=${GRAFANA_PASSWORD}

grafana.enabled=${GRAFANA_ENABLED}